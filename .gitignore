.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/
.DS_Store
.idea/
.vscode/

# 업로드 원본과 세션 데이터는 절대 커밋하지 않는다
uploads/
sessions.db
data/uploads/

# 로컬 LLM 모델 가중치 (수 GB) — 리포에 커밋하지 않는다. README 참고해 직접 받는다.
models/

# 평가 산출물
results/*.csv
reports/*.md
figures/*.png
!results/.gitkeep
!reports/.gitkeep
!figures/.gitkeep

# 스캔 PDF 페이지 렌더링 산출물. 원본 PDF 옆에 생기므로, 테스트가 fixture 를
# 직접 읽으면 tests/fixtures/ 안에도 만들어진다(테스트는 사본을 쓰도록 고쳤지만
# 실수로 되돌아가도 커밋되지 않게 막아 둔다).
_rendered/
