# 채무회복 내비게이터

저신용·다중채무자를 위한 근거 기반 AI 채무회복 실행 코파일럿.
**2026 금융 AI Challenge** (금융보안원 주최 · 제출 마감 2026-09-07 10:00) 출품작.

## 원칙

> **숫자는 확정, 제도는 후보.**
> 상환여력은 결정론적 계산 모듈이 확정 숫자로 산출한다.
> 제도 적합성은 판정하지 않고 근거·미확인 조건과 함께 검토 후보로만 제시한다.

## 배포

**https://debt-recovery-navigator.onrender.com**

조회서가 없어도 두 가지로 체험할 수 있다 — 업로드 화면의 **"데모용 합성 문서 선택"**
(생성 문서 13종) 또는 **"문서 없이 직접 입력"**. 시작은 반드시 `/` 에서 한다.

| 검증 항목 | 결과 (2026-09-02 실측) |
|---|---|
| `/healthz` | 200 · 0.38s |
| 데모 문서 → 실제 추출 | 채무 3건 정확 (LLM 백엔드 경유) |
| 전 구간 스모크 | ✅ 요약서 PDF 298KB 생성 |
| 375px 브라우저 | 10화면 · 가로 스크롤 0 · 콘솔 오류 0 |

호스팅은 Render 무료 플랜(카드 불필요), LLM 은 Groq 무료 티어를 쓴다. 절차·한도·
주의사항은 [`docs/배포.md`](docs/배포.md) 참고.

> ⚠️ 무료 플랜은 15분 무트래픽 시 잠들고 다음 요청에서 30~60초 걸려 깨어난다.
> `.github/workflows/keepalive.yml` 이 10분마다 깨운다(2026-09-03 `main` 병합 후
> 정상 작동 확인 필요 — Actions 탭에서 실행 이력 확인). **Render 배포 브랜치는
> 아직 대시보드에서 `main` 으로 옮겨야 한다** — 자세한 건 아래 [다음 단계](#다음-단계-우선순위) 참고.

## 현재 진행 상황 (2026-09-02 기준)

**`CODEX_TASKS.md`의 T00~T23 전체 23개 태스크 구현 완료.** 이후 팀원이 정책 카드
공식 출처 검증과 실서비스 결함 3건을 수정한 패치를 코드 레벨로 대조 검증해
[PR #1](https://github.com/EileenKwon/AI-Challenge/pull/1)로 `main`에 병합했다
(2026-08-26). 테스트 350개 전량 통과(`pytest -q`), `ruff check` /
`ruff format --check` 클린.

2026-09-02 작업으로 배포 전 체크리스트가 **배포 URL 하나만 남기고 전부 닫혔다** —
정책 카드 6종 전량 검증(`allow_unverified_cards: false`), 레드팀 `attacks.yaml` 전량
통과, 화면 02 입력 방식 4종 전부 구현, 375px 화면 7종 워크스루, Docker 빌드·기동 실검증.

작업 중 결함 2건을 발견해 고쳤다.

1. **`docker build` 가 실제로 깨져 있었다.** `python:3.11-slim` 이 Debian trixie 로
   올라가면서 `libgdk-pixbuf2.0-0` 패키지가 `libgdk-pixbuf-2.0-0` 로 바뀌어 빌드가
   `E: Package has no installation candidate` 로 죽었다. 그동안의 "정적 검토 통과"
   로는 잡을 수 없는 종류였다 — 베이스 이미지가 움직여서 생긴 문제이기 때문이다.
   패키지명을 고치고 베이스를 `python:3.11-slim-trixie` 로 **고정**했다.
2. **숫자 없는 LLM 응답이 설명문으로 그대로 노출**됐다. 375px 워크스루 중 결과
   화면에 `{}` 가 찍혀 있는 것을 발견했다. 상세는 `docs/평가결과.md` 참고.
3. **`DN_UPLOAD_DIR` 이 죽은 설정이었다.** `.env.example` 과 Dockerfile 이 이 변수를
   설정하고 있었지만 `Settings.upload_dir` 은 `config.yaml` 만 읽었다 — 아무 효과가
   없었다. 컨테이너를 비-root 로 내리면서 드러났다(쓰기 가능한 경로를 이 변수로
   지정했는데 앱은 여전히 `./uploads` 를 쓰다가 `PermissionError` 로 죽었다).
   이미 환경변수로 동작하던 `DN_SESSION_DB` 와 규칙을 맞췄다.

| 항목 | 상태 |
|---|---|
| 태스크 구현 (T00~T23) | ✅ 23/23 완료 |
| 단위·통합 테스트 | ✅ **372 passed** (2026-09-03 로컬 재확인) |
| 린트/포맷 | ✅ `ruff check`, `ruff format --check` 클린 |
| 검증 패치 PR | ✅ [PR #1](https://github.com/EileenKwon/AI-Challenge/pull/1)(2026-08-26), [PR #2](https://github.com/EileenKwon/AI-Challenge/pull/2)(2026-09-03) 전부 `main` **머지 완료** |
| 평가 하네스 (E1~E6) | ✅ 실행 완료 · 결과를 `docs/평가결과.md` 에 고정. E3 누락률 0.10 → **0.00**(정렬 결함 수정). E6 는 `attacks.yaml` 전량(14건)을 소비하도록 확장 — 이전에는 `boundary` 5건을 아무도 읽지 않았다 |
| Docker 빌드 검증 | ✅ 완료(2026-09-02) — **`docker compose build` / `up` 실행 검증.** 컨테이너 `healthy`, `scripts/smoke.sh` 전 구간 통과(요약서 PDF 415KB 생성 = 컨테이너 안에서 WeasyPrint·한글 폰트 동작 확인), `/demo-docs` 도 이미지 안에서 서빙됨. 이 과정에서 **빌드가 실제로 깨져 있던 것과 죽은 설정 1건을 발견**했다 — 아래 참고 |
| 컨테이너 비-root 실행 | ✅ 완료(2026-09-02) — 업로드 원본과 세션 DB 를 다루는 서비스라 `uid 1000` 사용자로 내렸다. 가변 상태는 `/app/var` 한 곳에 모아 그 디렉터리만 쓰기 권한을 준다(코드는 root 소유 유지) |
| 화면 02 입력 방식 4종 | ✅ 완료(2026-09-02) — PDF · 이미지 · 데모 문서 · **문서 없이 직접 입력** 전부 동작. 기획서의 4가지 방식이 모두 열렸다 |
| `Q5_RECENT_DEBT` 배선 | ✅ 완료(2026-09-02) — 실행일을 몰라 `recent_debt_ratio` 를 파생 계산할 수 없을 때 "신규채무 없음"(아니오) 답변만 비율 0 의 근거로 받는다. "있다"는 금액을 모르므로 미확인으로 남긴다 |
| 브라우저 수동 워크스루 (T19) | ✅ 완료(2026-09-02) — **375px 뷰포트 실제 브라우저(Chromium)로 7화면 클릭 워크스루 통과.** 10개 화면 상태 전부 가로 스크롤 0, 콘솔 오류 0 |
| 정책 카드 공식 출처 검증 | ✅ **6개 전부 `verified: true`** (2026-09-02). 개인워크아웃 총채무액 한도 충돌은 비교표 병합칸 오독이 원인임을 재현으로 확인하고, 제도 상세 페이지 원문값 15억(무담보 5억·담보 10억)을 조건으로 인코딩. `allow_unverified_cards` 를 **false** 로 전환 |
| 화면 02 데모용 합성 문서 | ✅ 완료(2026-09-02) — 13종 선택지를 `/demo-docs` 에서 받아 **일반 업로드 API 로 그대로 전송**한다(검증·PII 마스킹·인젝션 스캔 우회 경로를 만들지 않음). 조회서 없이도 끝까지 체험 가능 |
| 레드팀 `attacks.yaml` 전량 | ✅ 완료(2026-09-02) — 입력 공격 9/9 차단, 연체일수 경계 5/5 통과 |
| 설명문 방어선 보강 | ✅ 완료(2026-09-02) — 숫자가 없는 LLM 응답(`{}`·빈 문자열·영문 거절문)이 그라운딩 검증을 그냥 통과해 화면에 그대로 노출되던 결함 수정 |
| 실제 LLM(E1/E2 추출 정확도) 평가 | ✅ 완료(2026-09-02) — **실제 배포에 쓰는 백엔드(Groq 무료 티어 `qwen/qwen3.8-27b`) 기준으로 재측정.** E1 F1 **0.9979**(목표 0.90), E2 정확도 **0.9926**(목표 0.95). E1 은 **precision 이 전 필드 1.0000** — 없는 값을 지어낸 경우 0건이고, E2 의 잔액은 202/202 다. 백엔드를 바꾸면 둘 다 다시 재야 한다 |
| 필수생활비 하한 원문 대조 | ✅ 완료(2026-08-26) — 4·5·6인 값이 보건복지부 고시 제2025-135호 대비 낮게(각 13,193/24,415/33,640원) 잘못 들어가 있어 정정 |
| 무료 로컬 LLM 폴백 | ✅ 완료(2026-08-26) — `ANTHROPIC_API_KEY` 없이 Qwen2.5-7B(GGUF)로 동작. 사용법은 [로컬 LLM으로 무료 실행](#로컬-llm으로-무료-실행-anthropic_api_key-대안) 참고 |
| 세션 저장소 SQLite 배선 | ✅ 완료(2026-08-27) — `SqliteSessionStore`를 `get_session_store()`에 연결, `DN_SESSION_DB` 경로에 영속화. 프로세스 재시작 후에도 세션이 살아남는지 실측 확인 |
| 소득 감소 시나리오 배선 | ✅ 완료(2026-08-27) — 오케스트레이터가 `cashflow` 계산과 함께 `income_drop()`을 호출해 `AnalysisResult.scenario`를 채움. 화면 05에 "현재 vs 소득 감소 시" 비교 카드 추가 |

배포 전 남은 작업은 아래 [배포 전 체크리스트](#배포-전-체크리스트)를 따른다.

## 다음 단계 (우선순위)

마감 **2026-09-07(월) 오전 10:00** — 기획서(PDF)와 MVP 산출물(기능명세서 PDF · 웹서비스 URL)
이 같은 시각에 마감된다. 제출한 웹서비스 URL 은 **9/7 11:00 ~ 9/11 23:59 동안 접근 가능**해야
하며 접근 불가는 결격 사유다(대회 규정).

2026-09-03 팀원(이종민) 제안을 반영해 아래를 4단계로 정리했다. 근거·대상
파일 등 상세는 [`docs/고도화_아이디어.md`](docs/고도화_아이디어.md) 참고.

### Tier 0 — 지금 반드시 해야 할 것 (마감 전 필수)

| 순위 | 할 일 | 담당 / 비고 |
|---|---|---|
| P0 | **Render 대시보드에서 배포 브랜치를 `main` 으로 전환** | 코드로 못 하는 작업, 대시보드에서 수동 — [PR #2](https://github.com/EileenKwon/AI-Challenge/pull/2) 머지(2026-09-03)로 `main` 이 최신이 됐으니 이제 가능 |
| P0 | GitHub Actions에서 `keepalive.yml` 이 10분마다 실제로 도는지 확인 | Actions 탭에서 실행 이력 확인. 기본 브랜치(`main`)에서만 예약 실행되는데 이제 조건 충족 |
| **P0** | **기획서·기능명세서를 대회 제공 양식 PDF로 변환** | 규정상 제공 양식 파일 기반 PDF 제출. 저장소엔 `docs/기획서.md`·`docs/기능명세서.md` **원고만** 있음 — **현재 유일하게 안 끝난 제출 필수 산출물**. Tier 1 구현 완료 후 마지막에 한 번에 변환 (재작업 방지) |
| P2 | 개인워크아웃 한도 신용회복위원회(1600-5500) 전화 확인 | 공식 웹 원문(15억)으로 인코딩은 마쳤다. 채택값이 더 엄격한 쪽이라 자격 과대 안내 오류는 없어 급하지 않음 |

### Tier 1 — 마감 전 고임팩트 (심사 기준 "주제 적합성"에 직결)

| 순위 | 아이디어 | 예상 소요 | 한 줄 요약 |
|---|---|---|---|
| ★1 | 명분 재구성 | 반나절 | "조심스러워서"→"신용정보법 제36조의2가 요구하는 구조"로 프레이밍 전환 + 용어 교체 |
| ★2 | 보안 서사 뽑아내기 | 반나절 | 이미 있는 PII 마스킹·인젝션 스캐너·레드팀 14건·TTL·출력필터를 기획서 한 장으로 정리 (주최=금융보안원) |
| ★3 | 반사실 시뮬레이터 | 1.5일 | "연체 13일 더 지나면 구간 이동" 류 — swing_factors 기반 경계까지 거리 계산 |
| ★4 | 사용자 스터디 n=5 | 하루 | 미측정 지표 2개(사용자이해도·준비시간) 채우기 |

~~시간이 남으면: LLM 대조 실험, 접근성 "쉬운 말 모드"~~ **둘 다 완료(2026-09-03)** —
`eval/E4b_llm_direct_calc.py`(참고용, 이 환경은 STUB_MODE), 화면 05 "쉬운 말" 토글.
상세는 `docs/고도화_아이디어.md` 참고.

### Tier 2 — 마감 후 여유 시 (구조 강화)

지금 구조("계산은 순수 코드, 제도 판정은 YAML 조건표 조회, LLM은 추출·서술만")를
깨지 않는 선에서 더 단단하게 만드는 방향.

| 순위 | 아이디어 | 한 줄 요약 |
|---|---|---|
| 1 | 정책카드 OR 조건 그룹 | 지금은 조건이 전부 AND로만 묶여 "A거나 B" 제도를 못 담음 |
| 2 | 문서 추출 이중검증(운영 기능) | Claude·Groq 두 백엔드로 교차 검증, 값이 다른 필드는 UNKNOWN으로 강등 |
| 3 | 정책카드 원문 변경 감지 | 공식 페이지 diff를 사람에게 알림만 — 숫자는 여전히 사람이 직접 수정 |
| 4 | 그라운딩 검증 확대 | 금액 외 날짜·비율·기관명 환각도 방어 |
| 5 | confidence 보정 | E2 실측 정확도로 신뢰도 점수 교정 |
| 6 | 상담 연결 동선 완성 | 지역별 서민금융통합지원센터 안내 + 서류 체크리스트 PDF |
| 7 | 소득증빙 문서 추가 판독 | 급여명세서 업로드 지원 |
| 8 | 제도 비교 뷰 | 후보 3개를 감면율/기간/불이익으로 나란히 |
| 9 | 접근성 나머지 | 스크린리더 검증, 명도대비 AA |

### 하지 않기로 확정

- **조정 후 상환액 시뮬레이션** — "제도는 후보" 원칙을 정면으로 깨는 기능이라 금지 항목으로 못박음

## 전체 로드맵 (`CODEX_TASKS.md` T00~T23)

| # | 태스크 | 상태 |
|---|---|---|
| T00 | 프로젝트 부트스트랩 | ✅ |
| T01 | 도메인 모델 | ✅ |
| T02 | 세션 저장소와 상태머신 | ✅ |
| T03 | 문서 인제스트 | ✅ |
| T04 | PII 마스킹 · 인젝션 스캐너 | ✅ |
| T05 | LLM 클라이언트 | ✅ |
| T06 | 추출기 · 정규화 · 검증 | ✅ |
| T07 | 누락 · 모순 탐지 | ✅ |
| T08 | 현금흐름 계산 모듈 ★최우선 정확도 | ✅ |
| T09 | 소득 감소 시나리오 분석 | ✅ |
| T10 | 정책 카드 스키마와 로더 | ✅ |
| T11 | 3-state 조건 평가기 | ✅ |
| T12 | 규칙 엔진 | ✅ |
| T13 | 트리아지 | ✅ |
| T14 | 설명 생성기와 숫자 그라운딩 검증 ★환각 방어선 | ✅ |
| T15 | 안전 필터 | ✅ |
| T16 | 7일 행동계획 | ✅ |
| T17 | 상담용 요약서 PDF | ✅ |
| T18 | API 라우터와 오케스트레이터 | ✅ |
| T19 | 화면 7종 | ✅ |
| T20 | 합성 신용정보조회서 생성기 | ✅ |
| T21 | 평가 하네스 E1~E6 | ✅ |
| T22 | 설명가능성 번들과 감사 로그 | ✅ |
| T23 | 배포와 스모크 테스트 | ✅ |

## 폴더 구조

```
debt-recovery-navigator/
├── src/dn/                     # 애플리케이션 본체
│   ├── domain/                 #   Tracked[T] 도메인 모델, Money/Ratio 타입
│   ├── ingest/                 #   PDF 업로드·텍스트 추출
│   ├── llm/                    #   Anthropic 클라이언트 (API 키 없으면 StubClient 폴백)
│   ├── extraction/              #   LLM 구조화 추출·정규화·숫자 그라운딩 검증
│   ├── reconcile/               #   결측값·충돌 탐지, 보완 질문 생성
│   ├── cashflow/                 #   결정론적 현금흐름 계산 (순수 함수, float 금지)
│   ├── rules/                    #   YAML 정책 카드 로더 + 3-state 조건 평가기
│   ├── planning/                 #   7일 행동계획 생성
│   ├── narrative/                #   설명 생성기 (LLM 서술 + 그라운딩 검증)
│   ├── safety/                   #   입력/출력 안전 필터 (탈옥·리스크 조언·낙인 탐지)
│   ├── report/                   #   상담용 요약서 PDF (WeasyPrint)
│   │   └── templates/
│   ├── pipeline/                 #   9단계 analyze() 오케스트레이터
│   ├── api/                      #   세션·문서·분석 라우터
│   ├── web/                      #   Jinja2 + HTMX 화면 7종
│   │   ├── templates/
│   │   └── static/
│   ├── storage/                  #   세션 저장소, 상태머신
│   └── main.py                   #   FastAPI 앱 팩토리
├── config/
│   ├── config.yaml                #   설정 (allow_unverified_cards 등)
│   ├── policy_cards/v2026-08-13/  #   YAML 정책 카드 6개 (전부 verified: true)
│   └── safety/                    #   인젝션 패턴, 금지 문구 YAML
├── data/
│   ├── cases/                     #   테스트용 시나리오 케이스
│   ├── redteam/                   #   레드팀 공격 샘플 (attacks.yaml)
│   └── synthetic/                 #   합성 신용정보조회서 PDF + 라벨 (pdf/ 는 데모 문서로 런타임 서빙)
├── eval/                          #   평가 하네스 E1~E6 + report_builder
├── tools/synth/                   #   합성 데이터 생성 스크립트
├── tests/
│   ├── unit/ integration/ golden/ fixtures/
├── reports/                       #   metrics_summary.md 등 평가 결과 산출물
├── scripts/smoke.sh               #   데모 시나리오 curl 스모크 테스트
├── Dockerfile, docker-compose.yml #   배포 아티팩트 (빌드 미검증, §진행상황 참고)
├── ARCHITECTURE.md                #   시스템 아키텍처, 불변식, 모듈 설계
├── AGENTS.md                      #   코딩 에이전트 작업 규약 (절대 규칙 10개)
├── CODEX_TASKS.md                 #   T00~T23 순차 구현 지시서
└── docs/기획서.md                  #   서비스 기획서 (원본)
```

## 시작하기

```bash
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"
cp .env.example .env          # ANTHROPIC_API_KEY 설정 (없어도 StubClient로 동작함 — 아래 참고)
uvicorn dn.main:app --reload --app-dir src
```

API 서버가 실행되면 `http://127.0.0.1:8000/docs`에서 OpenAPI 문서를 확인할 수 있다.

**브라우저로 화면을 보려면** 루트 경로(`http://127.0.0.1:8000/`)에 접속한다.
랜딩 페이지에서 "새 세션 시작하기"를 누르면 세션이 만들어지며 소개·동의
화면(`/web/session/{session_id}/intro`)으로 이동한다. (세션 없이 화면
경로로 직접 들어가면 `404 Not Found`가 뜬다 — 반드시 `/`부터 시작한다.)

### ANTHROPIC_API_KEY 없이 개발하기

`ANTHROPIC_API_KEY`가 설정되지 않으면 `get_llm_client()`가 자동으로
`StubClient`로 폴백하고 `dev_mode`가 켜진다 (`src/dn/llm/client.py`). API
자체는 키 없이도 정상 동작하며(`eval/E1`·`E2`만 `[STUB_MODE]` 자리표시자
결과), `scripts/smoke.sh`·`tests/integration/test_smoke_flow.py`가 API를
직접 순서대로 호출하는 흐름은 키 없이도 끝까지(PDF 다운로드까지) 통과한다.

**브라우저 클릭만으로 화면 7종을 끝까지 진행할 수 있다.** 각 화면 폼에 `fetch`
기반 스크립트(`_layout.html`의 `dnPost`/`dnShowError`)를 붙여 API를 호출한 뒤
다음 화면으로 이동하도록 고쳤고, 보완입력(04) 화면은 폼 필드를
`SupplementRequest` 스키마(채무별 금리·월상환액, 소득/생활비 질문, 조건부
질문)에 맞게 변환해서 보내도록 다시 짰다. 분석(`/analyze`)은 보완입력
직후, 계획(`/plan`)은 경로(06) 화면의 버튼 클릭 시 자동으로 호출된다.

**화면 02의 4가지 입력 방식이 전부 동작한다.**

- **데모용 합성 문서 선택** — 13종 중 하나를 고르면 브라우저가 `/demo-docs/<케이스>.pdf` 를
  받아 일반 업로드 API 로 그대로 보낸다. 업로드 검증·PII 마스킹·인젝션 스캔을 우회하는
  별도 서버 경로를 만들지 않기 위해서다.
- **문서 없이 직접 입력** — 채권자만 필수이고 모르는 칸은 비워 두면 미확인으로 남는다.
  문서 경로와 같은 `S2_EXTRACTED` 로 도착하므로 이후 화면이 입력 방식을 구분하지 않는다.
  출처는 전부 `USER_INPUT` 이라 화면 배지와 설명가능성 번들에 "입력"으로 드러난다.
  **이 경로는 LLM 을 쓰지 않아 API 키 없이도 끝까지 동작한다.**

> ⚠️ 남은 알려진 제약
> - **LLM 백엔드가 없으면 문서에서 추출되는 채무가 0건이다.** `StubClient` 는 `{}` 를
>   돌려주므로 PDF·이미지·데모 문서 경로 3종은 화면이 끝까지 진행되어도 결과가 비어
>   있다. 배포에는 `ANTHROPIC_API_KEY` 또는 `DN_LOCAL_MODEL_PATH` 중 하나가 필요하다
>   (직접 입력 경로는 예외).
> - 보완입력(04)의 `Q5_RECENT_DEBT` 답변은 `recent_debt_ratio` 를 **파생 계산할 수 없을
>   때만** 쓰인다. "없다"는 비율 0 의 근거로 받고, "있다"는 금액을 모르므로 미확인으로
>   남긴다 — 자기신고만으로 제도 조건을 충족시키는 방향은 열지 않는다.

### 로컬 LLM으로 무료 실행 (ANTHROPIC_API_KEY 대안)

Anthropic API 자체는 이 프로젝트 규모(합성 문서 55건)면 실비가 $1 안팎으로
저렴하지만, 키를 아예 안 쓰고 싶다면 `llama-cpp-python` 기반 로컬 오픈모델로
대체할 수 있다. `get_llm_client()`의 우선순위는 **Anthropic 키 > 로컬 모델 >
StubClient** 다 — 키가 있으면 로컬 설정은 무시되고 그대로 Anthropic을 쓴다.

```bash
pip install -e ".[dev,local]"
mkdir -p models && curl -L -o models/Qwen2.5-7B-Instruct-Q4_K_M.gguf \
  "https://huggingface.co/bartowski/Qwen2.5-7B-Instruct-GGUF/resolve/main/Qwen2.5-7B-Instruct-Q4_K_M.gguf"

export DN_LOCAL_MODEL_PATH="$(pwd)/models/Qwen2.5-7B-Instruct-Q4_K_M.gguf"
uvicorn dn.main:app --reload --app-dir src
```

Docker로 돌릴 때는 `docker compose -f docker-compose.yml -f docker-compose.local.yml up --build`
(모델은 위 명령으로 미리 `./models`에 받아둔다 — 이미지에는 굽지 않고 볼륨으로 마운트한다).

- **모델**: Qwen2.5-7B-Instruct, Apache-2.0, 한국어 구조화 추출 성능 준수. E1/E2
  실측치는 `docs/평가결과.md` 참고 — 이 모델 기준이며 Anthropic 사용 시와는
  다를 수 있다.
- **CPU 스레드 수를 논리 코어 전체로 두면 안 된다.** 이 프로젝트 실측(Xeon
  w7-3465X, 56 논리 코어)에서 스레드 56개는 **0.29 tok/s**, 물리 코어
  근사치인 28개는 **18.5 tok/s**로 64배 차이가 났다 — 과도한 스레드 경합
  때문이다. `DN_LOCAL_LLM_THREADS`를 지정하지 않으면(`0`) 논리 코어의
  절반(최대 16)으로 자동 제한하지만, 배포 서버 사양에 따라 물리 코어 수를
  직접 넣어 튜닝하는 것이 안전하다.
- GPU 없이도 동작하지만 그만큼 느리다 — 위 튜닝된 스레드 수 기준으로 문서
  1건 추출에 수 초~수십 초가 걸릴 수 있다(문서 길이·CPU 성능에 따라 다름).

## 팀 개발 흐름

1. Issue에서 작업 범위와 완료 조건을 확정한다.
2. `main`을 기준으로 `feat/<issue>-<topic>` 또는 `fix/<issue>-<topic>` 브랜치를 만든다.
3. 코드와 테스트를 같이 작성하고 로컬 품질 검사를 통과시킨다.
4. PR 템플릿을 채워 리뷰를 요청하고, CI와 리뷰가 완료된 뒤 병합한다.

```bash
pytest -q
ruff check src tests eval
ruff format --check src tests eval
```

세부 규칙은 [`CONTRIBUTING.md`](CONTRIBUTING.md)와 [`AGENTS.md`](AGENTS.md)를 따른다.

## 문서

| 파일 | 내용 |
|---|---|
| `ARCHITECTURE.md` | 시스템 아키텍처, 불변식, 모듈 설계 |
| `AGENTS.md` | 코딩 에이전트 작업 규약 (절대 규칙 10개) |
| `CODEX_TASKS.md` | T00~T23 순차 구현 지시서 |
| `docs/기획서.md` | 서비스 기획서 (원본) |
| `docs/배포.md` | 배포 절차·검증 결과·무료 티어 한도 |
| `docs/고도화_아이디어.md` | 마감 후 시도해볼 개선 방향 6가지 |
| `CONTRIBUTING.md` | 브랜치, 커밋, PR, 테스트 규칙 |
| `reports/metrics_summary.md` | E1~E6 평가 결과 (기획서 14.3 지표 10개) |

## 제출물

| 항목 | 파일 | 상태 |
|---|---|---|
| 기획서 | `docs/기획서.md` | ✅ |
| **기능명세서** | `docs/기능명세서.md` | ✅ (2026-08-23 작성) |
| 평가 결과 | `docs/평가결과.md` | ✅ |
| **배포 URL** | https://debt-recovery-navigator.onrender.com | ✅ **배포 완료(2026-09-02)** |

## 배포 전 체크리스트

- [x] `config/config.yaml: rules.allow_unverified_cards` 를 `false` 로 변경 — **완료(2026-09-02)**. 개인워크아웃 한도 충돌 해소로 6종 전부 `verified`
- [x] 정책 카드 `verified: true` 및 `source.url` 기입 — **6개 전부 완료(2026-09-02)**. 개인워크아웃 한도 충돌 해소 근거는 카드의 `resolved_conflicts` 참고
- [x] `eval/E1~E6` 실측 — **실제 배포 백엔드 기준으로 전부 재측정 완료(2026-09-02)**. 9개 자동 지표가 같은 백엔드·같은 날짜로 정렬됐다 (`reports/metrics_summary.md`)
- [x] `data/redteam/attacks.yaml` 전량 통과 — **완료(2026-09-02)**. 입력 공격 9/9 차단, 연체일수 경계 5/5 통과. 이전에는 `boundary` 5건을 E6 가 읽지도 않았다
- [x] 업로드 원본 TTL 삭제 동작 확인 — **TTL 스위퍼를 앱 lifespan 에 배선(2026-08-23)**. 이전에는 정의만 되어 있고 호출부가 없었다
- [x] Docker 빌드/`docker compose up` 실제 환경에서 검증 — **완료(2026-09-02)**. 실행해 보니 베이스 이미지가 Debian trixie 로 올라가 `libgdk-pixbuf2.0-0` 이 없어져 **빌드가 깨져 있었다**. 패키지명 수정 + 베이스를 `python:3.11-slim-trixie` 로 고정. 이후 `up` → `healthy` → `scripts/smoke.sh` 전 구간 통과(요약서 PDF 415KB 생성)
- [x] T19 화면 7종 실제 브라우저(375px 포함)로 수동 워크스루 — **완료(2026-09-02)**. Chromium 375×812 에서 랜딩→계획까지 클릭만으로 통과, 가로 스크롤 0 / 콘솔 오류 0
- [ ] 배포 환경에 LLM 백엔드 설정 (`ANTHROPIC_API_KEY` 또는 `DN_LOCAL_MODEL_PATH`) — 없으면 추출 채무 0건
- [x] 화면 03 확인 상태 전송 — **채무가 1건이라도 있으면 `/confirm` 이 409 로 막히던 문제 수정(2026-08-23)**. 그동안은 StubClient 가 채무 0건을 반환해 우연히 통과하고 있었다

## 유의사항

본 서비스는 금융·법률 자문, 신용평가 또는 공식 자격심사를 제공하지 않는다.
최종 신청 가능 여부와 조정 조건은 신용회복위원회, 법원 등 공식기관을 통해 확인해야 한다.
