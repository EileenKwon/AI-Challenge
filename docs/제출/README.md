# 제출본

대회 제공 양식(HWPX)에 채워 넣는 최종 원고와 생성 도구.

| 파일 | 내용 |
|---|---|
| `기획서_양식본.md` | (첨부1) 기획서 7개 항목 |
| `기능명세서_양식본.md` | (첨부2) 기능명세서 5개 항목 |
| `../../tools/submission/` | 양식 치환 스크립트와 내용 원본(Python) |

## 생성 방법

```bash
# 대회가 배포한 빈 양식 HWPX 2개를 tools/submission/forms/ 에 둔 뒤
python3 tools/submission/fill_hwpx.py <출력디렉터리>
```

스크립트는 양식 XML 의 **안내문 문단**을 찾아 같은 서식의 문단 여러 개로 치환한다.
줄바꿈 위치 캐시(`hp:linesegarray`)는 넣지 않는다 — 한글이 파일을 열 때 다시 계산한다.

## 이력

- 2026-09-06 팀원 최종본(`실서비스반영_최종본`)을 기준으로 병합.
- 배포본과 대조해 사실 오류 2건을 고쳤다.
  - `새 세션 시작하기` → 실제 CTA(`개인정보 없이 2분 데모 체험` / `내 문서로 시작`).
    UX 개선으로 버튼이 바뀌었는데 문서가 옛 문구를 안내하고 있었다. 그대로 두면 심사자가
    첫 화면에서 막힌다.
  - `실행·만기일` → `실행일`. 추출 스키마에 만기일이 없고 `Debt.maturity_at` 을 채우는
    코드도 없다. 명세서는 구현된 기능만 적는 문서다.
- 팀원 판단 두 가지를 그대로 유지했다.
  - 표본 n=2 인 항목은 핵심 제출 수치에서 제외.
  - "외부에서 전체 E2E 를 독립 검증했다"고 표현하지 않는다. 자동 검증은 팀 내부 결과다.
- 2026-09-06 배포본·평가결과와 재대조해 3건을 더 고쳤다.
  - 레드팀 수치. `위험 조언 유도 9/9, 문서 인젝션 3/3` 은 두 집합을 나란히 써서 12건처럼
    읽히고, 9건 전부를 위험 조언으로 라벨했다. 실측(`results/e6_safety_redteam.csv`)은
    총 9건이고 인젝션 3건은 그 안에 포함된다 → `레드팀 공격 9건 전건 차단(문서 인젝션 3,
    위험 조언 유도 4, 역할 변경 유도 1, 타인정보 요구 1)`. 문서가 스스로 내건 "실측값과
    표본 수를 병기한다" 원칙을 어긴 유일한 숫자였다. `tests/unit/test_submission_claims.py`
    가 이 문구를 실측 CSV 에 묶어 재발을 막는다.
  - 검증 데이터의 `연체일수 경계값과 레드팀 공격 사례 14건`. 5+9=14 로 합은 맞지만 어느
    쪽이 14인지 읽히지 않아 `레드팀 공격 9건과 연체일수 경계값 5건` 으로 분리했다.
  - `최신 Chrome·Safari·Edge에서 동작하며`. 실제로 확인한 것은 Chromium 뿐이라
    `Chromium 계열(Chrome·Edge)에서 동작을 확인했고 Safari 등 다른 렌더링 엔진은
    별도로 검증하지 않았다` 로 범위를 좁혔다.

## 제출 전 남은 일

- [ ] 팀명·구성원 성명 입력 (문서에 `[입력 필요]` 로 표시돼 있다)
- [ ] 한컴오피스에서 열어 페이지 넘침 확인
- [ ] PDF 변환 후 글자 깨짐·잘림 확인
